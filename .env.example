# Copy to .env.local and fill. Never commit the filled version.

# --- Supabase (blocks B9, B10) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Migrations and the deploy script only. Never imported from frontend/app/**
# check:tenant-isolation fails the build if it is.
SUPABASE_SERVICE_ROLE_KEY=

# --- Later ---
RESEND_API_KEY=
UMAMI_API_URL=
UMAMI_API_KEY=
GOOGLE_PLACES_API_KEY=
META_APP_ID=
META_APP_SECRET=

# One per client, named in that client's config as sections.inquiryForm.accessKeyEnv
WEB3FORMS_ASHISH=
